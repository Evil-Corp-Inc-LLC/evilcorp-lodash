import { map, filter, reduce, groupBy, keyBy } from '../index.js';

const collection = { map, filter, reduce, groupBy, keyBy };

export { map, filter, reduce, groupBy, keyBy };
export default collection;
